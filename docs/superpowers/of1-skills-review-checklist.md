# OF1 Skills Review — Checklist (2026-08-11)

Branch: `skills-v5-next`. Recurring anti-patterns to watch: cloned-from-sibling boilerplate,
dead extracted vars, re-deriving upstream artifacts, DRY-violating pasted commands, correctness
rules living in soft-referenced prose instead of executed code.

## Phase 1 — Knowledge files (the contract; do first, it's the yardstick)
- [x] `knowledge/pipeline-contract.md` — audited. Gate is wired into both dispatch files (not soft). BUT found + fixed 2 real bugs in the gate script it cites (`check-replica-artifacts.mjs`): (1) assumed `progress.pages` is an array; stardust 0.18.1 writes an object keyed by slug → gate parsed nothing → normalizePages() now accepts both; (2) af1bb1a3 honest-fail bug — now exits 2 on pixelPct>10% OR verdict.overall==='fail', warns (not stops) on under-bar height/font-fork residuals. Legacy no-result shapes now flagged "fidelity UNVERIFIED". Contract exit table + both dispatch exit-2 descriptions updated.
- [x] `knowledge/dispatch-cc.md` — no live bugs (dep graph internally consistent, pipeline-mode timing correct). Fixed anti-pattern: Stage-3 dependency graph + model list were re-stated here (triple-dup w/ of1-integration + dispatch-slicc). Trimmed both to pointers at `of1-integration` § "Pipeline-mode timing" / model-assignment rule. Open: playwright-cli syntax prose (lines ~119) — check overlap w/ common-pitfalls in item 4.
- [x] `knowledge/dispatch-slicc.md` — same trim (was self-contradictory: said "use of1-integration as reference" then re-listed edges). Model line already terse/referenced, left. No live bugs.
- [x] `knowledge/common-pitfalls.md` — no live bugs: "min 4 images" threshold consistent across all enforcers (of1-extract-content gate + of1-publish Check 3), spec path §7.1 valid, curl/runtime/domain rules sound. Added canonical §9 "playwright-cli syntax" (was duplicated verbatim in ~5 skills w/ no home; copies agreed, no drift). dispatch-cc now points at §9. TODO in each skill's review (items 8-13): swap its playwright one-liner for a §9 pointer. Minor deferred: §2.2 says "up to 8 images" but download-images.mjs default maxPerProduct=5 (harmless, ≥4) → note for item 9; §7.6 token order omits OF1_TOKEN_FILE vs contract (partial).
- [x] `knowledge/worker-config-schemas.md` — validated every schema against real emitted configs. Fixed 1 real drift: line 102 said `template-select.js` picks "the 25 templates" (stale 5×5 era; of1-demo-lab-retro still has 25) — skill now makes 15 (5×3) and worker picks by intent from catalog candidates → reworded count-agnostic. Verified conformant: cta slots exactly [title,description,buttonText]; personas intentProfile 6-key vocab (explore/research/compare/purchase/deals/support) distinct-by-design from the 5 template intents (doc explains the bridge at L97-104); suggestions type enum matches of1-build-quick-suggestions (the `type:'explore'` seen in an old config is a pre-fix artifact, field is inert — not our bug). Required/optional table matches.
- [x] `knowledge/design-tokens-resolution.md` — clean, and the GOOD pattern: canonical home, all 4 citers (of1-integration, of1-build-templates, of1-build-cta-template, of1-style-generative-block) reference it and stay consistent (styles.css-canonical-for-live-site rule correctly encoded in templates Mode B). No live bug. Added the one implicit precondition the snippet omitted: paths are repo-root-relative (cd $OF1_DEMO_REPO first).
- [x] Cross-cutting: correctness-critical rules are mostly already in EXECUTED code, good posture:
  Stage 2 gate (check-replica-artifacts.mjs, now fixed), min-4-images (2 python gates: extract-content + publish Check 3), catalog=15 (publish Check 4), token resolution (publish/check-deps shell), and — strongest — publish Check 6 end-to-end generation test, which functionally validates emitted worker configs (so no separate field-level config-schema validator is warranted). The soft-referenced problems found were duplication/drift, now fixed: dep graph → canonical in of1-integration; playwright-cli → canonical common-pitfalls §9; "25 templates" → count-agnostic. Remaining follow-ups: (a) per-skill playwright pointer swaps during skill reviews (items 8-13); (b) lint-skills.sh deferred; (c) common-pitfalls §7.6 token order omits OF1_TOKEN_FILE vs contract — minor.

## Phase 2 — Skills not yet quality-reviewed
- [~] `of1-build-templates` FIRST PASS + user review done. Fixed: (1) base-CSS "owned by assemble" contradiction → base; (2) "~5×"→"~3×"; (3) user-invocable false→true; (4) stale "adopt-site" → of1-integration (also swept 5 other files repo-wide — adopt-site is not a real name); (5) sample.json image-URL rule now prefers of1/config/products.json images (real .aem.page/media URLs) over prototype HTML. Assets sound (deeper=item 19); fill-template cosmetic itemKeys item-6 vs item-9 log count. RELATED FIND: user-invocable still false on of1-discovery + of1-publish — same reasoning likely makes them true (decide during items 11/13).
- [x] `of1-extract-content` FIRST PASS + user review done. Fixed: (1) stale "25 templates" L199 → count-agnostic; (2) invocation now passes `--max-per-product 8` (matches "up to 8"; script default was 5); (3) testimonials.json — CONFIRMED consumed by worker (tenant.js loads it, build-template-prompt.js:62 passes it, template.njk fills quote slots) → added a full § to worker-config-schemas.md + required/optional row + extract-content schema-ref line; (4) DA_TOKEN preflight rewritten to walk the full 4-step order (ADOBE_IMS_TOKEN → OF1_TOKEN_FILE → $PWD/.hlx → $OF1_DEMO_REPO/.hlx) + guards "null". RELATED: of1-publish has the identical partial 2-step DA_TOKEN form — apply the same 4-step fix in item 11.
- [x] `of1-generate-config-review` — REPLACED by a runtime-render static asset (user-directed redesign). The skill + fill-config-review.mjs are DELETED. Now: `of1-integration/assets/config-review.html` fetches the live `/of1/config/*.json` on load and renders everything client-side (XSS-safe DOM APIs), incl. the previously-missing FAQs + Testimonials sections + a missing-config banner. of1-integration's "Config review" section inlines a light copy+commit+push (pushes `of1/config/` too, so configs are served at review time — publish alone runs too late). Domain comes from a new served `of1/config/config.json` (written by of1-check-dependencies §6; target domain can differ from EDS host). Step id renamed of1-generate-config-review→`config-review` across graph/dispatch/contract/orchestrator/extending-doc; removed from verify.sh REQUIRED_SKILLS + check-deps manifest. Covers asset item 17 (deleted). NOTE: config-review.html render not yet visually verified on a live deploy — JS syntax-checked + logic ported from the reviewed .mjs.
- [~] `of1-publish` FIRST PASS done. Fixed: (1) DA_TOKEN 2-step→full 4-step order (+null guard), matching extract-content; (2) playwright preamble → common-pitfalls §9 pointer; (3) completion summary "all 5"→"all 6" (6 checks); (4) BIG: `ready` criteria (L153) was wrong — corrected to the worker's actual isTenantReady (products+personas+use-cases+features+faqs+suggestions+of1-endpoint+cta-template ALL, plus block-guide OR templates; brand-voice NOT gated). This also corrected worker-config-schemas.md Required-for-ready table + brand-voice/block-guide prose (item 5's table was almost entirely wrong vs the worker). fill-demo-hub.mjs glanced: handles stages/steps alias, syntax OK (deep audit = item 16). Not audited: Step-1 soft presence check set, sync/generate curl error handling.
- [~] `of1-style-generative-block` FIRST PASS done. Fixed: (1) DA_TOKEN 2-step→full 4-step order (3rd skill w/ this); (2) ensure-nav-footer.mjs resolveToken widened to match (DA_TOKEN→ADOBE_IMS_TOKEN→OF1_TOKEN_FILE→$PWD/.hlx→$OF1_DEMO_REPO/.hlx) so a standalone .hlx-only run doesn't false-fail. Verified sound: no stale "25"; playwright used correctly (eval () => () form, no preamble to swap); Step-7 curl uses -d for short HTML (correct per §5.1); ensure-nav-footer handles Date-nondeterminism (OF1_YEAR), never overwrites, checks .plain.html. Covers asset item 20. FOLLOW-UP (user-directed): DEPRECATED the block-guide/non-template-routed styling entirely. Confirmed via SDK (of1-client.js L419-424) that template-routed content is styled by each template's own injected stylesheet; of1.css's `.generated-section .hero/.cards/.table/.columns` rules only applied to the dead block-guide path. Removed ~263 lines from of1.css (661→398) keeping only block UI (search/chips/skeleton/suggestions) + section container/fade-in + tokens; folded in the suggestion-restart flex fix. SKILL: reframed "Why", Step 2/3, deleted Step 4 "Verify block class names" + the whole "EDS Block DOM Reference" + selector-mistakes table + target-selectors; reframed Step 7b to a template-aware smoke test; trimmed Key principles + mistakes table; renumbered to 0,1,2,3,4,5,5b,6,7,7b; fixed of1-publish external Step ref. Added guardrails: do NOT style generated-section internals. Earlier tombstone/adventure-cards/renumber also done.
- [~] `of1-discovery` FIRST PASS done. Fixed: playwright preamble → common-pitfalls §9 pointer. Verified sound: no dead vars (OWNER/REPO/BRANCH/DOMAIN all used), narrative.json domain field IS consumed (dispatch-slicc reads it), slug rules consistent, crawl bounds consistent. DONE (user-approved): standardized discovery.html like demo-hub. Built `assets/fill-discovery.mjs` + `assets/discovery-report.html` (themed template). Script reads of1-discovery-output.md (minimal dep-free MD→HTML renderer, drops the duplicate H1), copies discovery-*.png → deliverables/assets/screenshots/ referenced by absolute path (no base64 bloat), writes deliverables/discovery.html. Step 5 now calls the script (no hand-authoring). Wired SKILL_DIR: added to discovery Env table + Stage-1 dispatch on both runtimes (dispatch-cc export note; dispatch-slicc scoop env + project-context) since it was previously only exported to Integrate-stage skills. Verified end-to-end w/ synthetic md+screenshots: headings/lists/links/bold/img all render, hierarchy correct.
- [x] `of1-signals` FIRST PASS done. Well-written standalone operator tool — correctly OFF the pipeline (no orchestrator/verify.sh dep), self-contained, consistent with the "not in worker-config-schemas" note, no playwright, thorough schema + priority-order docs. Only fix: Step 5 said "Commit and push" with NO git commands (every other skill shows them) → added explicit path-scoped commands (git add of1/config/signals.json, not `git add .` per §6) + noted it rides the code bus (no DA upload/preview needed). No dead vars, no stale refs.
- [x] `of1-demo-orchestrator` SKILL body FIRST PASS done. Clean overall — correct runtime detection, nesting-cap explanation, Phase 0, and all knowledge-file pointers; config-review rename propagated correctly (inline at L84/100/121). One fix: L96-103 re-stated the full Stage-3 dependency graph while L106-107 says it's "defined once in of1-integration, not a reimplementer" (same self-contradiction/drift vector trimmed in the dispatch files) → trimmed to orientation-level flow + pointer. No dead vars/stale refs.

## Phase 3 — Executed code (the reliable tier — bugs here actually run)
- [x] `of1-publish/assets/fill-demo-hub.mjs` DEEP AUDIT done. Sound: template tokens exactly match the replacements map (no orphan/missing), --orange defined, stages/steps alias + object-or-array normalization + path fallbacks all handled. Fixed: removed dead getLogoSvg (no {{LOGO}} token — it read logo.svg then `void`d it). Verified syntax + functional smoke (no unreplaced tokens in output).
- [x] `of1-generate-config-review/assets/fill-config-review.mjs` — DELETED (replaced by runtime-render config-review.html; see item 10).
- [x] `of1-extract-content/assets/download-images.mjs` — reviewed + simplified: `--input` is now optional; when omitted the script derives the manifest directly from `--products-json` (default of1/config/products.json), removing the redundant python-heredoc manifest projection + /tmp/image-manifest.json temp file from the SKILL. Backward-compatible (--input still works). Verified: syntax, arg guard, manifest derivation picks only products with images[], token resolver order intact.
- [x] `of1-build-templates/assets/assemble-catalog.mjs`, `fill-template.mjs` DEEP AUDIT done. assemble-catalog.mjs: clean (inlines htmlContent, byIntent map, missing-intent warn, stylesheet-as-path matches schema). fill-template.mjs: well-hardened (nested-tag refusal, safe href/escaping, Finding 47/52/54 fixes). Fixed: itemKeys stopped at item-6 while card-hide + slot convention support item-1..9 → extended to item-9 (was a log-count undercount only; card-hide regex already handled all via `\d+`).
- [x] `of1-style-generative-block/assets/ensure-nav-footer.mjs` — reviewed w/ item 12; widened token resolution to full 4-step order. Sound otherwise (never-overwrite, .plain.html check, Date-safe).
- [x] `of1-check-dependencies` verify.sh + SKILL audited. verify.sh: shellcheck-clean (warning level), no bugs — token/skill-root resolution sound. SKILL.md: found + fixed 2 real bugs in the clean-slate/restart section: (1) DANGEROUS — bare `git add -A` (L124) violates common-pitfalls §6 and could wipe the repo on a partial SLICC tree → scoped to `git add -A -- <cleaned paths + quoted globs>`; verified it stages only intended deletions and leaves out-of-scope files unstaged; (2) `sed -i '/^of1/d'` non-portable (fails on BSD/macOS = this env) → `sed -i.bak … && rm -f .bak`. Confirmed no other bare `git add -A/.` or non-portable `sed -i` in any skill.

## Already done (do NOT redo)
- of1-extract-brand-voice (dead DOMAIN, source block) ✓
- The skill-name identity migration (all skills) ✓
- of1-integration structural cleanup ✓
- Playwright shim removal, of1-adopt→of1-integration rename ✓

## Deferred (named so they don't get lost)
- `scripts/lint-skills.sh` — enforcement gate; write AFTER reviews so it encodes findings
- of1-labs rollout stage 3 — container step-tracker + suggest.js still emit numeric/"explore"
